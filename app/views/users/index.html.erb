<%= form_tag(upload_users_path, multipart: true, class: 'w-50 d-flex gap-3 align-items-center justify-content-center') %>
<%= file_field_tag :file, class: 'form-control', accept: 'text/csv', required: true %>
<%= submit_tag 'Upload CSV', class: 'btn btn-primary' %>

<%= link_to "Back to groups", groups_path, class: 'btn btn-light' %>
